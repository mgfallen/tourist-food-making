server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index swagger.html;
    }

    location /swagger {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /index.html;
    }
}
