server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index swagger.html;
    }

    location /swagger {
        root /usr/share/nginx/html/swagger;
        try_files $uri $uri/ /index.html;
    }

}
